/*import { TestBed, inject } from '@angular/core/testing';

import { AppInstallationApiService } from './app-installation-api.service';

describe('AppInstallationApiService', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [AppInstallationApiService]
    });
  });

  it('should be created', inject([AppInstallationApiService], (service: AppInstallationApiService) => {
    expect(service).toBeTruthy();
  }));
});
*/
