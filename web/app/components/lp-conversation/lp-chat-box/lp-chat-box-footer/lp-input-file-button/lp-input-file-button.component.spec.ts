/*import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { LpInputFileButtonComponent } from './lp-input-file-button.component';

describe('LpInputFileButtonComponent', () => {
  let component: LpInputFileButtonComponent;
  let fixture: ComponentFixture<LpInputFileButtonComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ LpInputFileButtonComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(LpInputFileButtonComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});*/
