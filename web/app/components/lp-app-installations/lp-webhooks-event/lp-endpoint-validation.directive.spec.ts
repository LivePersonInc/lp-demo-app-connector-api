import { LpEndpointValidationDirective } from './lp-endpoint-validation.directive';

describe('LpEndpointValidationDirective', () => {
  it('should create an instance', () => {
    const directive = new LpEndpointValidationDirective();
    expect(directive).toBeTruthy();
  });
});
