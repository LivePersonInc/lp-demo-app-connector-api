import { LpDetailsScopeValidationDirective } from './lp-details-scope-validation.directive';

describe('LpDetailsScopeValidationDirective', () => {
  it('should create an instance', () => {
    const directive = new LpDetailsScopeValidationDirective();
    expect(directive).toBeTruthy();
  });
});
