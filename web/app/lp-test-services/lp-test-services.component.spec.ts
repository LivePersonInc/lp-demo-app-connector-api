/*import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { LpTestServicesComponent } from './lp-test-services.component';

describe('LpTestServicesComponent', () => {
  let component: LpTestServicesComponent;
  let fixture: ComponentFixture<LpTestServicesComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ LpTestServicesComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(LpTestServicesComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
*/
